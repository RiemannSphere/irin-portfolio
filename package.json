{
  "name": "minimal",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "deploy:local": "npx wrangler pages dev ./dist",
    "deploy:prod": "npx wrangler pages deploy ./dist"
  },
  "dependencies": {
    "astro": "^5.11.1"
  },
  "devDependencies": {
    "wrangler": "^4.25.0"
  }
}
